name: nirmadapp
description: "NIRMAD FLUTTER APP"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # Firebase
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  cloud_functions: ^6.0.0
  # File uploads (native Firebase Storage APIs with progress)
  # State management & DI
  flutter_riverpod: ^2.6.1
  # Routing
  go_router: ^16.2.1
  go_transitions: ^0.8.2
  animations: ^2.0.11
  responsiveness: ^1.2.8
  # Models & serialization
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  # Utils
  flutter_dotenv: ^6.0.0
  intl: ^0.20.2
  package_info_plus: ^8.3.1
  file_picker: ^10.3.2
  image_picker: ^1.2.0
  url_launcher: ^6.3.0
  file_saver: ^0.2.12
  dio: ^5.7.0
  path_provider: ^2.1.5
  csv: ^6.0.0
  excel: ^4.0.3
  pdf: ^3.11.0
  printing: ^5.13.4
  cross_file: ^0.3.4+2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^1.0.0
  timelines_plus: ^1.0.7
  cupertino_sidebar: ^1.0.1
  # UI/UX enhancements
  flutter_form_builder: ^10.2.0
  form_builder_validators: ^11.2.0
  flutter_animate: ^4.5.0
  gap: ^3.0.1
  # cached_network_image removed in favor of hash_cached_image
  hash_cached_image: ^0.0.2
  avatar_better_pro: ^0.0.4
  image: ^4.3.0
  shared_preferences: ^2.3.2
  flutter_svg: ^2.2.0
  font_awesome_flutter: ^10.7.0
  google_fonts: ^6.3.1
  flutter_offline: ^5.0.0
  connectivity_plus: ^6.1.5
  # Maps & Location (free/open-source stack)
  flutter_map: ^8.2.1
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  flutter_map_location_marker: ^10.1.0
  # Toasts
  toastification: ^3.0.3
  logger: ^2.6.1
  fire_storage_impl: ^0.1.0
  path: ^1.9.0
  flutter_slidable: ^4.0.1
  introduction_screen: ^4.0.0
  permission_handler: ^11.3.1
  open_filex: ^4.4.0
  mime: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  flutter_native_splash: ^2.4.4
  icons_launcher: ^3.0.0

flutter_native_splash:
  color: "#FFFFFF"
  color_dark: "#000000"
  image: "assets/1152 x 1152.png"
  image_dark: "assets/1152 x 1152.png"
  # branding: "assets/dhamtari_district_administration_logo.png"
  android: true
  ios: true
  web: true
  android_12:
    # Use the smaller asset that has more natural padding to avoid circular cropping
    image: "assets/640 x 640.png"
    image_dark: "assets/640 x 640.png"
    icon_background_color: "#FFFFFF"
    icon_background_color_dark: "#000000"

icons_launcher:
  image_path: "assets/1152 x 1152.png"
  platforms:
    android:
      enable: true
      # Use adaptive icon with white background and a padded foreground to avoid cropping
      adaptive_background_color: '#FFFFFF'
      adaptive_foreground_image: "assets/Nirmad icon.png"
      adaptive_monochrome_image: "assets/Nirmad icon.png"
      notification_image: "assets/640 x 640.png"
    web:
      enable: true
      image_path: "assets/1152 x 1152.png"
      favicon_path: "assets/favicon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true
  assets:
  - assets/
  - .env
  - assets/logo.png
  - assets/login_page_graphics.svg
  - assets/login_page_graphics_2.svg
  - local_data.json

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
